# I-02：确定性执行分支与恢复

> 当前实现、37 项更新映射和未决验收见文末“当前实现与验收状态”。以下首段保留 `7834bdb` 的历史结论，不代表后续工作仍未实现。

## 7834bdb 历史结论与交接点

**BLOCKED：送达回执交错的规格解释待产品治理判断；I-02 未完成。** 本轮是实施前覆盖盘点与风险证据 checkpoint，没有改动产品代码，没有完成三个工作包，也没有达到“只等待 Windows 验收”的阶段。

任务为独立 implementation，沿用 [Issue #33](https://github.com/Annzival/ADHD-Support-System/issues/33)。按任务的规格歧义退出条件保存最小复现和候选处理，回传直接委托者；没有自行修改产品范围、领域模型或 ADR。未启用产品治理角色，不进入 I-03 或 dogfooding。

2026-09-23 的只读启动检查：

- 工作树干净；未发现本任务既有分支或 PR。起初位于已合并的交接分支，未向它或 I-01 分支提交。
- PR #32 已合并，merge `9fcd7663e640822bc3bfacc97c93a3d5aefa848f`。
- 交接 PR #34 已合并，远端 main 为 `afd6eaaef23840a8a64be11391dab6b5c3a8810c`；远端交接文件 Git blob 为 `388d44d48e8a092ab562ddcc1f50b50f1bf962cb`。
- 从该最新 main 建立 `agent/implement-mvp-deterministic-recovery`。Issue #33 正文仍描述等待交接合并，以本轮评论补充实际状态，不擅改任务契约。
- 已读 AGENTS、CONTEXT、发布规则、I-02 交接、B-01～04 相关五份产品文档、ADR-0004/0008/0043～0056，以及期限与干预引用 ADR-0012/0013/0016/0022/0023/0025/0026/0039；核对 I-01 运行说明、结果、正式 Core／宿主／前端及现有测试、锁定版本，参考四份 Wails／关闭恢复／localhost／SQLite spike 结果。

当前生产代码仍为上述 main 基线。新增 `desktop/i02_delivery_probe_test.go` 仅为显式开启的诊断，不参与确定迟到回执的产品语义；普通测试运行会跳过它。它验证已确定的不变量并输出实际状态，不断言 `cancelled` 是本争议顺序的正确产品结果。

## 实施前覆盖清单

下表是**编码前基线盘点**，不是 I-02 完成表。“已有”指代码和旧 I-01 范围；“缺失”包括实现、完整断言或集成证据缺口。所有 Windows 项均需最终 I-02 构建的独立证据，不能复用旧 PASS。本轮仅重跑原有回归和新增诊断。

测试缩写：G = `tests/acceptance/test_execution_golden_path.py`；A = `test_atomicity.py`；D = `test_delivery_and_recovery.py`；T = `test_transport.py`；UI = `desktop/tests/frontend.test.mjs`；宿主 = `desktop/delivery_test.go`、`bridge_test.go`。完整预期继续以 [B-04](../../product/mvp-acceptance-scenarios.md) 为准。

| 场景 | 已有实现／证据 | 缺失参数分支及断言 | Windows |
| --- | --- | --- | --- |
| EX-01 | G/A：到点、开始、完成、完成／跳过收尾；唯一活动位置；旧 I-01 实机 | I-02 全链路及多个安排的可操作入口回归 | 待实机 |
| EX-02 | G/UI：仅打开、取消、缺确认、确认本次时长；会话与检查点联合创建 | I-02 新前端回归，取消不得改变权威状态 | 待实机 |
| EX-03 | 无 | 已有估时仍须确认剩余时长；确认／取消／不确认；来源区别、实际开始未知 | 待实现后实机 |
| EX-04 | 无 | 回顾式完成→完成／跳过收尾；无检查点、未知现实时间、不计促成进入 | 待实现后实机 |
| EX-05 | 无 | 具体改期；旧安排与新后继同事务、引用保留、旧送达失效；故障回滚 | 待实现后实机 |
| EX-06 | 无 | 只结束当前安排与决定事实；其他安排不变；无后继、会话、包及行动完成 | 待实现后实机 |
| EX-07 | D/宿主：重复 tick／回执、多载体同身份、丢包重试一次呈现 | 第二次较弱跟进、无第三次、机会去重；本轮发现回应先于回执的歧义 | 待实机 |
| EX-08 | D：离线首次尝试失效的部分基础 | 跟进时安静／离线／下一工作已开始，三支均不补发且保持未知 | 待实现后实机 |
| EX-09 | D：到窗口时旧操作拒绝 | W／T0+D／本地次日零点三支的持久过期；期限早于宽限；无显著性升级及补记 | 待实现后实机 |
| EX-10 | A：两个安排竞争活动位置 | 无活动／有活动两支的单一前台；旧主窗口保留、旧置顶与跟进停止 | 待实现后实机 |
| SES-01 | 无 | 到点续行、后继链、同 ID 重试、无确认；保持会话 ID、原检查时间和唯一当前检查点 | 待实现后实机 |
| SES-02 | G：检查点前／后 × 完成；等待收尾占位置，旧检查点失效 | 检查点前／后 × 暂停；旧续行拒绝；新界面回归 | 待实机 |
| SES-03 | G/UI：全部／部分结果、原报告保留、草稿断连保持 | 后续方案版本变化后的确切历史引用、不回写事实 | 待实机 |
| SES-04 | 无暂停；完成分支关闭／跳过有旧基础 | 暂停后完成／显式跳过／关闭三支的最小证据和恢复包同事务；可选未知 | 待实现后实机 |
| SES-05 | 无 | 完成／暂停 × W／下一前台／本地零点；多边界最早者；幂等、无额外提醒 | 待实现后实机 |
| SES-06 | 检查点首次送达与窗口外操作拒绝基础 | 较弱确认一次、W／检查点日期次日零点；未知跟踪结束，无收尾／证据／包 | 待实现后实机 |
| SES-07 | 无 | 追加更正内容／原因／时间、保留原证据；按更正后的明确事实校验恢复 | 待实现后实机 |
| REC-01 | D/T：未确认旧尝试失效、同库身份／有效调度基础 | 持久恢复记录、来源不变复用、错过多项只一个恢复上下文 | 待真实 PC 重启 |
| REC-02 | D：未过期被动开始保留，重启不补发 | 重启未到／已过保留期的持久转换、仍保留行动 | 待真实 PC 重启 |
| REC-03 | G/T：未来检查点同会话恢复 | 有效被动核对／核对期限已过两支；未知退出幂等、不重计时 | 待真实 PC 重启 |
| REC-04 | G/T：等待收尾状态可持久恢复 | 未到／已到边界各重启两次；一次自动收束与证据／包去重 | 待真实 PC 重启 |
| REC-05 | 无 | 包→当前新安排／新会话／检查点／已使用联合转换；重复请求；再次暂停关联新包 | 待实现后实机 |
| REC-06 | 无 | 旧版本包：旧断点／当前计划两支；不移动版本指针，未使用归档不改历史 | 待实现后实机 |
| REC-07 | 无 | 已使用／已归档／已明确完成／已有活动会话四支拒绝；不部分消费包 | 待实现后实机 |
| REC-08 | 无 | 只有错过开始：处理上一项／暂不决定／重启后被动返回；不得虚构断点或再催促 | 待实现后实机 |
| REC-09 | 无 | 会话／包／安排／当前版本各来源变化；旧操作拒绝、旧记录失效、新记录按需创建 | 待实现后实机 |
| REC-10 | 无 | R09 明确切换+新检查时间；A 未知退出且无证据／包，B 唯一占位置 | 待实现后实机 |
| REC-11 | 无 | 取消／未确认／故障／同 ID 重试四支；A 已进入等待收尾时旧切换拒绝 | 待实现后实机 |
| REC-12 | T 仅有 I-01 开始的响应丢失基础 | R09 提交丢响应→同库重启重试；旧通知／旧检查点拒绝，不重建或重送达 | 待真实 PC 重启 |
| DESK-01 | 正式 Wails 已有单实例／托盘／关闭钩子；UI 模拟关闭回归 | I-02 正式构建再次启动、主窗留托盘、小窗关闭恢复和置顶观察 | 待实机 |
| DESK-02 | 宿主已有受限守护、同库 Core 重连 | 正式开机启动配置／清理与真实 PC 重启步骤；单宿主／Core；I-02 REC 整合 | 待实机 |
| DESK-03 | T/宿主：回环 HTTP/WS，缺失／错误／旧令牌拒绝，Core 重连 | I-02 构建协议回归和新恢复状态集成；无 SQLite 壳访问继续保持 | 待实机 |
| DESK-04 | D/宿主：当前／过期通知核验，旧 I-01 焦点记录 | 改期／其他新解决分支的旧通知；多行动正确上下文；本轮交错风险 | 待实机 |
| DESK-05 | 无恢复界面 | 两侧信息独立展开、主窗口主入口、通知跳转、小窗无选择、暂不决定安静返回 | 待实现后实机 |
| AT-01 | A：开始／完成报告／完成和跳过收尾逐写故障及提交前进程退出 | 回顾式完成／改期／今天不做／续行／暂停收尾／包使用／R09，以及新增自动转换的联合写入故障 | Linux 缺项补齐后仍需实机恢复 |
| AT-02 | A/T/UI/宿主：上述 I-01 命令与送达的不确定响应、同 ID 重试 | I-02 新增每类命令提交丢响应＋同库重启；对象／事件／调度不重复 | Linux 缺项补齐后仍需实机恢复 |
| AT-03 | A：同种命令竞争；完成收尾／跳过竞争；唯一位置 | 开始／改期、续行／完成、恢复／恢复、切换／完成四对不同命令；过期载体无副作用 | Linux 缺项补齐后仍需实机恢复 |

AT-01/02 的方案启用事务留 I-03；本表没有将其遗漏当作 I-02 缺陷。旧 I-01 测试原样保留，本轮未将 `operation_not_available_in_i01` 的拒绝测试误算为新分支通过。

## 回执交错的最小复现

方法：真实 Python Core、SQLite 临时数据库、HTTP、Go `deliveryPump`；可控领域时钟固定为 100，夹具 T0=99、D=60、W=1000。使用宿主现有发送器，仅将原生提交替换为返回成功的回调，在指定边界提交用户开始命令。此处“呈现成功”是测试输入，不是 Windows 原生通知已实测的结论。

各支步骤：首次尝试 pending/v1 → 宿主领取 claimed/v2 → 呈现回调一次 → 按表中顺序提交回执与用户开始 → 三次新快照驱动 pump → 显式重试相同回执命令 → 核验旧用户上下文。

| 顺序 | 观察到的送达记录 | 相同回执命令再次提交 | 成功事实 |
| --- | --- | --- | --- |
| 回执先提交，再用户回应 | delivered/v3，delivered_at=100 | 200，原处理结果 | 有 delivery_result_recorded |
| 呈现回调内先提交用户回应，再返回呈现成功 | cancelled/v3，delivered_at=null | 409 stale_context | 无 delivery_result_recorded |
| 呈现后回执请求丢失，再用户回应 | cancelled/v3，delivered_at=null | 409 stale_context；pump 见取消后停止回执工作 | 无 delivery_result_recorded |
| 回执已提交但响应丢失，再用户回应 | delivered/v3，delivered_at=100 | 200，原处理结果；pump 按原 ID 重试 | 有且仅有一次 delivery_result_recorded |

四支均只有一次呈现、一个执行中会话和一个检查点（due_at=160），检查点引用同一会话；原开始调度 fired、新检查调度 scheduled；无执行结果证据、现实开始／结束仍未知；旧用户上下文均 409。显式回执重试前后的快照完全相等。完整事件、对象版本和引用在受控原始输出中，脱敏摘要见 [interleaving.json](mvp-deterministic-branches-recovery.interleaving.json)。

当前机制：`Core.cancel_deliveries` 将 pending 和 claimed 都取消；用户开始同时将干预 pending/v1 解决为 resolved/v2。回执仍引用 delivery/v2、intervention/v1，先被送达记录版本守卫拒绝。即使只改送达版本，干预版本守卫仍会拒绝；宿主也会在看到取消后终止回执工作。不能以单独“重试更多次”解决。

没有发现两会话或半更新；问题是原生成功输入与持久送达事实是否允许在这个顺序下分离。开始成功不证明它是由通知促成，也不能从用户回应倒推出成功送达。

## 待治理判断：G-01

这是一处待解释的规格边界，**尚未认定现有 ADR 必然互相冲突**：

- A02 要求“对应干预及送达尝试身份有效”，通用规则要求预期状态版本校验。现有实现因回应后版本变化拒绝迟到回执，符合严格版本守卫的解释。
- ADR-0052 及领域模型规定回应取消“尚未执行的送达尝试”；已经呈现、回执未提交的尝试是否仍属于该集合未明确。ADR-0016 还要求分别保存送达与回应事实。
- 若在回应后补记送达，必须明确这是历史设备事实的特殊接纳边界，不能因此恢复旧用户操作、调度或再次呈现。规格未规定其接纳资格、时间依据，以及机会记录如何处理回执在进入会话之后到达的情况。

候选处理（均未实施）：

1. **仍拒绝迟到回执**：当前版本守卫不变；明确原生呈现与 Core 确认送达不保证一致，未确认呈现只保留诊断、不计机会。需要治理明确接受此语义及证据限制。
2. **有限追加设备呈现事实**：只针对先前合法领取且可关联的同次尝试，允许保存已经发生的呈现结果；已解决上下文仍不可操作、不重新调度、不重复计数。需要治理明确身份／版本例外、时间依据、失效／重启和去重规则，然后再实现并验证。

需要决策的是“上述第 2/3 行已呈现但尚未被 Core 确认的尝试，回应后是否接纳其历史回执，以及依据是什么”。本 session 不替治理选定，不新增补偿规则或放宽旧操作权限。

任务 Prompt 明确要求此类歧义保存证据后回传。本轮据此在产品编码前退出；独立的不受影响工作仅完成覆盖盘点和 I-01 回归。没有将 G-01 宣称为所有其他转换的技术依赖；后续可在明确协调后继续不依赖它的工作包，但完整验收不能绕过它。

## 实际运行与结果

从仓库根目录运行（Go 1.25.0 在 PATH；Python 3.12.3）：

```bash
I02_DELIVERY_PROBE=1 go -C desktop test -race -count=1 -run '^TestI02DeliveryInterleavingEvidence$' -v
python3 -m unittest discover -s tests/acceptance -v
I02_DELIVERY_PROBE=1 go -C desktop test -race -count=1 ./...
go -C desktop vet ./...
npm test --prefix desktop
git diff --check
```

Windows PowerShell 可以运行相同的平台无关诊断，但它仍使用原生回调替身：

```powershell
$env:I02_DELIVERY_PROBE = '1'
# 若 python3 不存在，指向已核验的锁定解释器：
$env:I01_TEST_PYTHON = 'D:\Tools\Python312\python.exe'
go -C desktop test -race -count=1 -run '^TestI02DeliveryInterleavingEvidence$' -v
Remove-Item Env:I02_DELIVERY_PROBE
Remove-Item Env:I01_TEST_PYTHON
```

上面的路径需替换为操作者已核验的 Python 3.12.3 x64；Windows race 测试还依赖本机 C 工具链，若不可用不应为本轮诊断升级锁定环境，可去掉 `-race` 并明确证据边界。本轮未在 Windows 执行此命令。

Linux 实际工具路径与补充浏览器依赖沿用 I-01 本机产物；使用 `.scratch/toolchain/go/bin/go`。浏览器命令实际附带 `I01_TEST_GO`、`I01_BROWSER_EXECUTABLE` 和 `LD_LIBRARY_PATH`，不新增依赖或改锁定版本。

| 检查 | 本轮结果 | 限制 |
| --- | --- | --- |
| Python | 17 个测试方法通过 | 只证明 I-01 既有范围 |
| Go race | 原 7 个入口通过，加 1 个诊断入口的 4 支 | 诊断执行成功不是争议语义验收 PASS；最终诊断修改后单独重跑 race |
| Go vet | 通过 | 无原生 Windows 运行 |
| 浏览器 | 6 项通过，两个页面的收尾草稿回归保留 | 首次因本机库路径遗漏 `extracted` 导致浏览器未启动；修正调用环境后通过，两份输出均保留；不是产品缺陷修复 |
| I-02 产品功能 | 未完成／BLOCKED | 三个工作包尚未实现 |
| I-02 Windows | 未执行 | 原生通知、焦点、五项宿主与 PC 重启没有本轮证据 |

环境、生产源码 commit、探针源码哈希、原始输出 SHA-256 和四支摘要保存在同目录 JSON。原始输出受控保存于本 checkout 的 `.scratch/i02-checks/`，不进 Git；临时数据库由测试清理。审查者可从任务执行者取得原始输出并核验摘要哈希，至少保留至本任务复审结束。旧 I-01 各轮 JSON 和 Windows 结果没有修改或合并为新一轮测试。

## 未交付项与恢复工作位置

- 工作包一：开始分支、策略／跟进／期限、单一前台、桌面入口及相应 AT。G-01 结论确定后，把诊断转为对应正确性断言，保留所有控制支与旧回归。
- 工作包二：续行链、暂停包、自动收束、未知退出、旧版本引用和追加更正。
- 工作包三：恢复记录、包使用／归档、R09、桌面整合、开机启动、真实 PC 重启。
- 仍需隔离多版本／期限／策略夹具；不能把合成夹具升级为正式设置功能。
- 仍需最终产品代码、各分支状态／事件／调度／引用断言、运行说明及可复制的 I-02 原生 Windows 手册／脚本。当前 `windows-smoke.ps1` 不支持 I02，不应传 `-Stage I02`；未交付手册不能记为等待操作者回传。

本 checkpoint 仅供审查风险证据，不建议作为完成的 I-02 合并；PR 保持 Draft。Issue #10 保持已关闭，不自行 Ready、merge、关闭 PR、发布或进入下一阶段。

## 2026-09-24 继续实施：G-01 仅局部待决

已正常合入 main `7ba8f05`（含 PR #36 / ADR-0057、PR #37 沟通规则），保留 `7834bdb` 历史及上文当时结论。当前不再以 G-01 阻塞整个任务。推荐方案见 [G-01 最小处理方案](../plans/i02-late-delivery-proposal.md)，尚未启用新生产接纳逻辑。

沿用上文 37 项：EX-01/07 与 AT-02 仅迟到结果子支确实依赖 G-01；其他缺项均为当前可以实现，已有 I-01 范围保持回归；DESK-01～05 和各恢复原生表现等待最终 Windows 实机验证，不与 Linux 实现状态混写。后续 checkpoint 在本节之后追加逐项验证，不覆盖旧诊断摘要。

### 确定性实现 checkpoint（2026-09-24）

已加入开始校正／回顾完成、改期、不执行、期限与单一前台；续行链、暂停包、显式／自动收尾、未知跟踪结束、追加更正；恢复来源版本校验、包使用／归档、R09 原子切换。相应主窗口／小窗入口已实现，恢复选择只在主窗口。宿主新增当前用户登录启动开关，Core 离线后重连重新核对恢复来源。未启用 G-01 候选历史回执规则。

本轮检查：Python 39 个方法通过（含逐写故障、9 类新联合命令提交前进程退出、四对命令竞争、四类恢复来源变化）；浏览器 11 项通过（原 6 项保留，新增真实 Core 页面分支）；原 Go 回归及四支诊断 race 通过，vet／Windows amd64 交叉构建／PowerShell 语法通过。这里只报告已执行自动层，不声称全部参数化验收或 Windows PASS。

修复过程保留：首次检查点因两次读时钟产生微小偏差，改为同一进入时刻派生；小窗结束后补显示保存结果；浏览器测试适配层关闭复用连接，解决用例之间的连接重置。原 I-01 的重启测试仅增加对新恢复事件的明确断言，仍要求原状态／原事件不重复；旧开始尝试不补发与新恢复呈现分别核对，未跳过旧回归。

下一检查点补完整 Windows 操作手册、逐项场景证据映射及最终检查摘要。G-01 仍局部待主线程确认；当前不能宣称“仅等待 Windows”。

## 当前实现与验收状态（2026-09-24，源码 f50af6e）

**整体仍为 BLOCKED／未 PASS：G-01 的历史结果接纳条件尚待主线程确认，Windows 原生验收尚未执行。** 不依赖 G-01 的三个工作包已实现并完成下列自动检查，不再把这项规则缺口当作其他功能的阻塞。实现可开始审查；PR #35 保持 Draft，不能据本报告直接认定 I-02 完成或启动 I-03。

历史链：`7834bdb` 保存原始诊断 → `57cedc2` 正常合入已合并决定 → `433c2ed` 提交 G-01 推荐方案 → `7b4201d` 保存三个工作包的共享事务、领域和桌面实现 → 本次补恢复通知取消、通知上下文定位、遗漏断言、Windows 手册及最终映射。各工作包相互依赖同一事务模块，本次共享实现保存在同一代码 checkpoint；没有重建分支、改写历史或覆盖旧证据。

### 当前 37 项覆盖清单

“自动通过”只指表中参数和测试层；原生操作另列，不是整行 Windows PASS。L = `tests/acceptance/test_i02_lifecycle.py`（方法名包含场景编号）；G/A/D/T/UI/宿主沿用上文缩写。L 每个临时库结束时同时检查会话唯一性、当前检查点、事件命令去重、调度与目标、方案／安排／会话／证据／包引用和恢复记录唯一性；各分支另外检查业务结果。完整快照相等用于证明拒绝、故障和同 ID 重试没有副作用。

| 场景 | 已实现并验证／测试入口及参数 | 仍需处理 |
| --- | --- | --- |
| EX-01 | G 全链路；UI 原开始／完成／两类收尾保留；L 新分支同一活动位置 | Windows A；迟到送达与开始先后归因依赖 G-01 |
| EX-02 | G 未确认、取消、确认；UI 主窗／小窗时长输入和取消，无半会话 | Windows B |
| EX-03 | L `ex03`：已有估时仍确认剩余时长、拒绝未确认、来源与实际开始未知；UI 确认／取消 | Windows B |
| EX-04 | L `ex04`：回顾完成后全部／部分／跳过，无检查点、时长未知；UI 回顾入口 | Windows C 两轮 |
| EX-05 | L `ex05`：后继、原时间和行动引用、旧送达取消；AT 逐写故障；UI 确认及取消改期 | Windows D |
| EX-06 | L `ex06`：仅当前安排与决定事实，其他安排和行动不变，无会话／包／后继；UI | Windows D |
| EX-07 | L `ex07_08`、D/宿主：正常首次回执、最多一次较弱跟进、不产生第三次，原请求／响应丢失重试保留 | 迟到结果子支确实依赖 G-01；Windows E 原生强弱 |
| EX-08 | L `ex07_08`：宽限时安静／离线／下一项到来，不补发，无推断结果 | Windows E 的原生部分；其余受控时钟自动验证 |
| EX-09 | L `ex09`：窗口／T0+D／本地次日零点，期限早于宽限；过期旧入口拒绝，行动仍未知 | Windows F；无窗口入口可另用 WithoutWindow |
| EX-10 | L `ex10`：有／无活动会话时单一前台，旧入口被动保留、取消旧跟进；UI 多安排 | Windows G 焦点与置顶 |
| SES-01 | L `ses01`：同会话后继链、原检查时间、同 ID 重试；另测三种缺确认保持完整快照；UI 续行 | Windows A |
| SES-02 | G 完成前／后检查点；L `ses02` 暂停前／后、旧续行拒绝；等待收尾仍占位置；UI | Windows A/B |
| SES-03 | G 全部／部分及原报告；L `ses03` 后续合成版本指针变化不改旧事实；UI 原两个页面草稿断连回归 | Windows A/C；版本变化是隔离夹具，不是 I-03 方案启用测试 |
| SES-04 | L `ses04` 完成／显式跳过暂停收尾、最小包和未知进展；UI 暂停后宿主关闭钩子 | Windows B 原生关闭 |
| SES-05 | L `ses05_and_rec04` 完成／暂停 × 窗口／下一项／零点，最早边界、重复恢复无重复证据／包；自动命令逐写故障 | Windows F；零点由可控时钟验证 |
| SES-06 | L `ses06_rec03` 检查点首次及一次较弱跟进、窗口／检查点日期零点未知结束，无证据／包 | Windows F |
| SES-07 | L `ses07` 追加内容／原因／时间，原事实保留、逐写故障与重试；`rec06` 更正后完成拒绝恢复；UI | Windows A |
| REC-01 | L `rec01_02_08_09`、D/T：同库身份、未确认旧尝试失效、来源不变复用；UI 重连 | Windows H 真实 PC 重启 |
| REC-02 | 同上：未过期被动保留／已过期未知，不重放；L `ex09` 三种期限 | Windows F/H |
| REC-03 | G 同会话未来检查点；L `ses06_rec03` 被动核对和过期未知；`return_same_session` 返回不重计时并取消恢复呈现 | Windows F/H |
| REC-04 | L `ses05_and_rec04` 未到／已到边界重启，完成／暂停去重；G 等待收尾持久化 | Windows F/H |
| REC-05 | L `rec05_06` 包→新安排／会话／检查点／已使用，再次暂停新包关联旧包；AT 同 ID 重试；UI 包继续 | Windows B |
| REC-06 | L `rec05_06` 旧 P 继续仍引用 P、当前 Q 不移动；`rec06_archive` 未使用归档原事实不变；UI 两个主窗选择 | Windows G |
| REC-07 | L `rec07` 已使用、已归档、已明确完成、有另一活动会话均拒绝，完整快照不变 | Windows G 可观察拒绝 |
| REC-08 | L `rec08_defer` 仅错过开始、暂不决定后重启安静、无虚构会话；UI 被动返回 | Windows G |
| REC-09 | L `rec09_each_source` 会话／包／安排／当前版本变化四支，旧命令拒绝且新来源重核；UI 读取新状态 | Windows G/H；版本切换由合成夹具隔离 |
| REC-10 | L `rec10_11_12` R09 原子切换，A 未知无证据／包，B 独占；UI 真 Core 切换入口 | Windows H |
| REC-11 | L 缺两类确认、逐写故障、同 ID 重试、旧切换不得覆盖完成报告；UI 取消／确认 | Windows H |
| REC-12 | L 提交后重开同库原 ID 返回原 B，旧 A 检查点失效；T 提交丢 HTTP 响应；宿主原通知核验 | Windows H 真实重启及旧通知 |
| DESK-01 | 宿主原单实例／托盘／关闭机制保留；UI 两页关闭收尾先保存，失败可重试 | 等待 Windows：小窗置顶／恢复、单实例、关闭主窗留托盘 |
| DESK-02 | 受限 Core 守护保留，增加当前用户登录启动配置／清理及真实重启脚本；Windows 交叉构建通过 | 等待 Windows：守护次数、登录启动、同库 PC 重启 |
| DESK-03 | T/宿主：回环 HTTP/WS、令牌与重连；新命令白名单仅 Core 执行；前端真实 HTTP | 等待 Windows 宿主整合，Wails 不访问 SQLite |
| DESK-04 | Core 对过期／解决／改期旧上下文拒绝；宿主通知向 Core 核验，主窗定位目标；UI 状态刷新 | 等待 Windows 通知实际跳转／焦点；迟到结果接纳另依赖 G-01 |
| DESK-05 | UI 主窗两侧独立展开、暂停包／切换／暂不决定；小窗仅打开主窗、不承载恢复选择 | 等待 Windows 载体、原生通知辅助返回 |
| AT-01 | A 旧命令保留；L 9 类新增联合命令每次写入故障及提交前子进程退出；另测更正／归档、过期／自动收尾／未知结束／跟进每次写入回滚 | Windows 同库进程／PC 恢复；方案启用不属本阶段 |
| AT-02 | A/T/UI/宿主旧重试；L 新联合命令、自动命令、更正／归档提交后重开同库同 ID 返回同结果、整库快照不变 | G-01 未提交迟到结果确实待决；Windows 恢复 |
| AT-03 | L 不同命令四对：开始／改期、续行／完成、包恢复／包恢复、切换／完成；A 旧竞争保留 | Windows 表现；SQLite 竞争已在 Linux 真实临时库验证 |

没有仍以“当前可以实现”搁置的已知独立功能；该状态的条目已推进至实现和表列自动证据。表中的原生行为以及 G-01 子支尚未通过，不合并成 37/37 PASS。UI 关闭钩子、通知回调替身与 Windows 原生事件分别计证。

### 运行与证据

最终命令（在仓库根目录；使用既有本机锁定工具，不安装或升级环境）：

```bash
/usr/bin/python3 -m unittest discover -s tests/acceptance -v
PATH="$PWD/.scratch/toolchain/go/bin:$PATH" I02_DELIVERY_PROBE=1 go -C desktop test -race -count=1 ./...
.scratch/toolchain/go/bin/go -C desktop vet ./...
GOOS=windows GOARCH=amd64 CGO_ENABLED=0 .scratch/toolchain/go/bin/go -C desktop build -o ../.scratch/i02-desktop.exe .
I01_TEST_GO="$PWD/.scratch/toolchain/go/bin/go" I01_BROWSER_EXECUTABLE=/home/Parzival/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome LD_LIBRARY_PATH="$PWD/.scratch/browser-libs/extracted/usr/lib/x86_64-linux-gnu" npm test --prefix desktop
.scratch/powershell/pwsh -NoProfile -File scripts/acceptance/tests/test-python-discovery.ps1 -TestPython /usr/bin/python3
git diff --check
```

工具路径可替换为操作者已经核实的同版本安装，不能因此把 Linux PowerShell 当作 Windows 宿主验收。Windows 可复制的操作、预期与结束点见 [I-02 Windows 手册](../windows-i02.md)；正式入口现已支持 `-Stage I02`，取代上文历史 checkpoint 的“脚本尚未实现”说明。

本轮 Python 46 个测试方法、浏览器 13 项（零跳过）、Go race（含四支诊断）、Go vet、Windows amd64 交叉构建和 6 支 PowerShell 解释器发现检查均通过。自动层未发现失败项；这不抵消表中 G-01 和 Windows 缺口。

最终源码 commit、环境、原始输出哈希、测试数量及构建哈希见 [本轮实现证据摘要](mvp-deterministic-branches-recovery.implementation.json)。旧 [交错证据](mvp-deterministic-branches-recovery.interleaving.json)原样保留；本轮诊断输出只作回归观察，不重新命名为接纳规则验收。所有原始输出保留在本 checkout 的 `.scratch/i02-checks/` 至复审结束；仓库只保存摘要与 SHA-256，不保存原始库、个人内容或令牌。

最后补充修复：返回原会话时取消尚未执行的恢复通知任务，避免选择后仍被旧恢复通知打扰；通知返回主窗口时定位对应当前上下文。未放宽任意版本检查或历史回执接纳。

### 交回主线程与 Windows 操作者

主线程仅需决定三个产品结果：用什么证据认定回执属于之前允许发送的那次提醒、顺序不明时保存到什么程度、未提交结果可跨哪些重启接纳。推荐方案已在 [G-01 文档](../plans/i02-late-delivery-proposal.md)分别列出 Core 持久许可、宿主内存发送结果、两种时间及六种事件顺序；推荐顺序未知时不推断促成进入、未提交结果不跨进程轮次接纳。字段和表结构等可逆实现细节无需用户选择。该推荐尚不是生产规则。

未发现新的确定 ADR 冲突；G-01 是 ADR-0057 已接受原则下的接纳条件缺口。待主线程确认后仍需实现并把争议诊断改为明确验收断言，再结合 Windows 本轮证据才能判断整体 PASS。Windows 操作者可先执行其他场景，不必等待 G-01；本 session 没有 Windows 操作条件，到此保存检查点，不无限等待或外推 Linux 结果。
