# G-01：已接受提交边界的验证交接

任务类型：technical spike 的契约复验与 Windows 能力核验；不是继续寻找严格协调方案。依据 [ADR-0058](../../adr/0058-allow-inflight-delivery-result-commit-after-host-restart.md)，原规则被有限调整；两轮 FAIL 与研究保持原样。沿用独立 I-02 任务、Issue #33、原实现分支和 Draft PR #35。

## 完整续接 Prompt

使用条件：包含 ADR-0058 及本交接的治理 PR 经人工合并后执行。未合并时返回等待，不自行采用未合并规则。

```text
继续现有 I-02，本轮只验证 ADR-0058 已接受的新契约及锁定 Windows 的原进程实例机制，不再研究严格提交协调，也不启用生产接纳规则。你是独立技术执行 session，不继承产品治理身份。

准备：检查工作树、Issue #33、Draft PR #35，沿用 agent/implement-mvp-deterministic-recovery；核实本交接已合并再正常合入 main，保护现有修改与历史。完整阅读 AGENTS.md、CONTEXT.md、发布规则、ADR-0055／0057／0058、mvp-acceptance-scenarios.md 的 G01-R/U/C 场景，以及两轮实验和提交边界研究报告。旧报告结论保持原样。

验证问题：同一结果事务通过关联及原宿主实例存续核验后，能否按新规则完成保存，同时保持检查前已退出拒绝、资格不跨事务或 Core 重启继承、已提交原命令返回及不重发？Windows 句柄机制是否确实绑定原宿主实例？

范围内：
- 复用现有隔离实验；按 ADR-0058 新建有版本依据的断言／结果。原 exit_after_check_before_commit 在新契约下应允许一笔保存，但旧 commit 和两轮 FAIL 不回写；逐项解释新旧预期的差别。
- 原 38 支按新契约回归；补 G01-C03：检查成功后事务回滚、旧宿主退出再重试应拒绝；Core 在未提交时重启应拒绝，恢复库有原命令时则返回原结果。已提交、未提交必须依据数据库而非 HTTP 响应判断。
- 继续断言错误关联、检查时已退出、身份或存续依据不足拒绝；同一报告去重、冲突拒绝、无半更新、无新发送，生产任务／会话／调度快照不变，顺序未知不计机会或精确延迟。
- 明确首次打开句柄时如何对应原宿主实例，不能只证明“后来一直持有这个句柄”。核对登记与打开之间退出／身份复用的反例；可信本机宿主前提不扩展为防御同权限恶意进程，不能静默以随机标签或 PID 相等代替依据。需要最小隔离适配时可实现；若无法可靠绑定，保存证据并返回，不放宽规则。
- 沿用锁定 Windows 10 22H2 x64、Python 3.12.3、Go 1.25.0 环境，先完成 Linux 契约复验，再交付 Windows 最小可复制步骤。实机只核验进程身份／存续与隔离事务路径，不要求重复全部 Wails 操作或 PC 重启；原生通知等完整 I-02 验收仍另行完成。

范围外：不改生产 schema、路由或正式回执守卫，不新增启动仲裁、宿主持久结果库或权限，不修改 ADR／产品范围，不开展精确先后分类、I-03/I-04 或浏览器间歇失败调试。

验收与退出：
- 每支给出新契约预期、实际、证据层级及源码 commit；Linux 与 Windows 分层记录。首次绑定、检查前退出、检查后宿主重启允许、回滚后重试、新 Core 拒绝及已提交查询均有证据；保存始终只发生一次，不改变领域状态。
- 矩阵完成即回传。缺 Windows 条件时交付步骤、预期、退出／清理及脱敏收集方式，保存 checkpoint 后报告等待 Windows，不把交叉编译当运行通过。
- 同一路径连续三次无新证据或需要改变产品／架构边界时停止并回传。没有证据的时长上限不得补写为承诺，不为“消除最后检查之后的空隙”继续改造。
- 本轮通过仅证明候选满足新契约；生产集成的同事务一致性和完整 I-02 仍须后续实现与验收，未经新授权不自动进入生产实现。

交付：新增 docs/implementation/results/i02-g01-accepted-boundary-validation.md 及独立机器摘要，记录环境、源码、命令、逐支结果、哈希与限制；链接但不覆盖两轮 FAIL／研究。更新 I-02 总报告、Issue #33、Draft PR #35；checkpoint 后 push，多行 GitHub 正文使用 UTF-8 文件与 --body-file 并读回。保持 Draft，不 Ready／merge／关闭 Issue。

回传：分层 PASS／FAIL／BLOCKED 或等待 Windows、证据、剩余风险、有无范围／ADR 冲突。由当前治理线程核对后再授权正式接纳实现，不宣称 I-02 PASS。
```
