# G-01：重启边界与提交协调的有界研究

状态：已完成，研究回传 `baf72b1`。用户随后接受有限放宽，决定见 [ADR-0058](../../adr/0058-allow-inflight-delivery-result-commit-after-host-restart.md)，下一步按[新契约验证交接](i02-g01-accepted-boundary-validation.md)。下文是历史研究任务，不重复执行，也不继续寻找严格协调方案。

任务类型：research，不是第三轮修复或 production implementation。第二轮源码 `9848b29`、证据 `86b6941` 的[报告](https://github.com/Annzival/ADHD-Support-System/blob/86b6941/docs/implementation/results/i02-g01-restart-revalidation.md)表明，保留进程实例句柄仍未消除最后检查至提交之间的竞争。原 30 支通过，新增 8 支中 7 PASS、1 FAIL。

治理已核对计数、10 个源码文件与 8 份原始输出的哈希／长度，并独立重跑 `TestG01RestartRevalidation/exit_after_check_before_commit`，重现应 409／零报告、实际 200／一份报告。未重跑全矩阵、未完成 PR #35 全量代码审查，也未验证 Windows。接受 FAIL 证据；不推断所有方案均不可能，也不授权再加检查或事后删除事实来换取通过。ADR-0057 不变。

## 完整续接 Prompt

本交接经人工合并后交给已有独立 I-02 任务；沿用 Issue #33、实现分支及 Draft PR #35。该轮只产出研究结论，不自动启动技术实验或实现。

```text
继续现有 I-02，本轮仅做 G-01 重启边界与数据库提交协调的 research。你是独立研究／技术执行 session，不承担产品治理，不修改产品范围或 ADR。

准备：
1. 检查并保护现有工作树、Issue #33、Draft PR #35；沿用 agent/implement-mvp-deterministic-recovery，保留两轮 FAIL 证据。确认本交接已人工合并到 main 后正常合入，不重写历史；未合并则返回等待。
2. 完整读取 AGENTS.md、CONTEXT.md、ADR-0055／0057、docs/agents/grilling-git-workflow.md、本交接、两轮 i02-g01 结果报告和机器摘要，以及当前实验的事务与进程身份代码。使用可用的 research 技能；原有实验只读查阅。

唯一研究问题：在 Windows 10 单机、Python Core 唯一写入 SQLite、Wails 薄宿主且进程可能异常退出的约束下，是否存在有一手依据、成本适合 MVP 的机制，保证旧宿主重启前尚未提交的结果不会在重启后提交，同时保留已提交原命令查询？

范围内与方法：
- 先明确事件：设备返回、请求到达、Core 接纳检查、SQLite 事务提交、旧宿主退出、新宿主开始运行、Core 观测退出／新宿主登记。不能混用这些时刻，也不能把正常退出协议当成强制终止保证。
- 使用 Microsoft 进程／同步文档、SQLite 事务／提交文档及必要的 Python 官方资料。每项关键主张给出链接、适用前提和限制，区分资料原文保证、根据代码得出的反例和研究推断。
- 最多深入比较两个方向：一个尝试保持当前严格边界的协调机制；另一个明确承认行为变化的较弱边界，仅作为候选。若找不到有依据的严格方案，报告“在已检查的约束和方案内未找到”，不要从两次失败宣称普遍不可能。
- 对严格候选说明谁能够阻止哪一个事件、以哪个事件决定先后，以及异常退出、强制结束、新宿主尚未登记、检查后提交前重启和 Core 自身重启时的反例分析。仅缩短窗口、重复检查、异步退出通知或承诺正常关闭流程不足以证明消除当前反例。
- 若候选依赖改变进程所有权、共享持久状态、宿主访问数据库、同步保证或启动边界，逐项标明与现架构／ADR 的冲突，不默许这些变更；不要用新架构掩盖成本。
- 对较弱候选用具体时间线说明：哪些原本拒绝的旧结果会被保存；哪些仍拒绝；任务／会话是否受影响；是否重发；机会计数和延迟如何保持未知；窗口是否有可证明上限。没有证据不得称为“仅几毫秒”或“没有用户影响”。它必须标为未接受，不能成为测试新预期。

范围外：
- 不编写、修改或运行新实验，不改生产代码／schema／测试断言／ADR；不做第三轮无假设修补，不新增遥测、宿主持久事实库、权限或平台升级。
- 不开展 I-03/I-04，不顺带调试浏览器间歇失败。精确发送先后仍非保存未知顺序报告的前提。

验收与退出：
- 提交最多两项方案的保证／前提／反例／架构成本表，以及一条建议下一步：有依据的唯一最小 spike（附验证问题和退出条件），或需要用户决定的具体行为取舍，或证据不足。
- 能明确区分严格边界的可行性与放宽规则的代价即结束；最多两轮定向资料核对，连续一轮没有新增关键依据则停止并列明未知，不扩展成架构选型项目。
- 资料不能回答且必须运行代码时，只定义最小实验并回传，不自动执行。研究结论不等于 Windows 实测，也不等于 I-02 PASS。

交付：
- 中文报告 docs/implementation/results/i02-g01-commit-boundary-research.md，记录来源、访问日期、前提、证据与推断、候选比较、限制及具体推荐；不要覆盖两轮失败历史。
- 更新 Issue #33、Draft PR #35 和 I-02 总结果的入口；checkpoint commit 后 push，多行正文通过 UTF-8 文件及 --body-file 发布并读回。保持 Draft，不 Ready／merge／关闭 Issue。
- 返回“有依据的可验证候选／需产品取舍／证据不足”之一及证据链接。候选未经治理接纳不得转入实验或生产。由当前明确激活的治理线程判断是否影响 MVP、领域模型或 ADR；如需改变行为，必须交用户明确决定。
```
