# I-02 G-01：关联与顺序依据的有界验证

任务类型：technical spike，作为现有 I-02 中与正式实现隔离的验证阶段。沿用独立 I-02 session、Issue #33、`agent/implement-mvp-deterministic-recovery` 与 Draft PR #35；不由治理线程执行，也不从头重建已有任务。产品取舍见 [ADR-0057](../../adr/0057-allow-late-delivery-facts-without-reopening-actions.md)，验收预期见 [G01-R／U 补充场景](../../product/mvp-acceptance-scenarios.md)。候选提案位于 PR #35，不作为已通过的证据。

## 第一轮回传与治理核对

PR #38 已合并为 `065e8fb`。第一轮隔离实验源码 `62bc99a`、证据 `eb3863b` 已在 [PR #35](https://github.com/Annzival/ADHD-Support-System/pull/35) 回传 **FAIL：29／30 支通过**；[报告](https://github.com/Annzival/ADHD-Support-System/blob/eb3863b/docs/implementation/results/i02-g01-verification.md)和机器摘要仍在未合并的实现 PR，不声称已进入 main。

治理核对：30 支计数一致，8 个来源文件和 7 份原始输出的 SHA-256／输出长度与摘要匹配；只读核对实验端点及失败断言，并独立重跑 `TestG01RestartBeforeRegistration`，仍得到预期 409／零报告、实际 200／一份报告的失败。未重跑其余 29 支或全部产品回归，也未完成 PR #35 全量代码复审。接受该反例足以否定当前候选机制，不接受该机制进入生产。

问题是 Core 只知道最后登记的宿主身份，无法覆盖旧宿主退出、新宿主运行但尚未登记的空档。维持 ADR-0057，不将“新宿主登记”改称“宿主重启”，不重新询问已确认取舍。29 支通过只支持隔离实验中的关联、去重等能力，不证明生产集成原子性或 Windows 行为。另保留浏览器首轮两支 `UND_ERR_SOCKET`、重跑通过但原因未定位的回归稳定性风险；本次重启复验不顺带调试它。

下一步只做下文的宿主重启识别复验，仍属 technical spike，不是生产实现授权。现有领域规则、MVP 范围与 ADR 不变；若证据表明必须改变边界，先带回具体反例与代价。

## 第一轮 Prompt（历史，已执行完毕）

使用条件：PR #38 经人工合并后再执行。若仍未合并，只核对已有工作并返回等待，不从未合并治理分支借用起点。该验证结束必须回传，不能自动进入 G-01 生产实现。

```text
继续现有 I-02 任务，本轮只做 G-01 的有界 technical spike。
你是独立技术执行 session，不是产品治理主线程；只生产证据，不更改产品范围、领域模型或 ADR。

准备：
1. 检查当前工作树、Issue #33 和 Draft PR #35。沿用 agent/implement-mvp-deterministic-recovery 分支及已有工作，保留 7834bdb、f50af6e、50b17d8 历史。不新建重复任务，不改写历史、不覆盖用户修改。
2. 核实 PR #38 已合并且本交接已进入远端 main，再正常合入最新 main；有无法安全处理的冲突先回传。不自动 Ready／merge。
3. 完整阅读 AGENTS.md、CONTEXT.md、docs/agents/grilling-git-workflow.md；ADR-0052、0055、0057；docs/product/mvp-domain-transitions.md、mvp-acceptance-scenarios.md；docs/implementation/plans/mvp-deterministic-branches-recovery.md、本交接及 i02-late-delivery-proposal.md。核对后者仍属候选，不能覆盖已确认决定。

已确认边界：
- Core 与宿主均未重启时，能核对到此前允许发送的同一次尝试的设备结果，即使发送与用户回应先后不明也保存；顺序及机会资格未知，不据此计机会或精确延迟。
- Core 或宿主任一进程重启后，不补存此前未提交结果；已提交记录及同命令 ID 的原结果返回保留。关闭窗口但进程仍运行不算重启。
- 无对应许可、不能关联尝试或只有用户点击，不构成设备发送结果依据。保存历史报告不授权新发送，不恢复旧操作、重开干预、修改会话或重复计数。API 成功不证明用户看见。

验证问题：
1. 以现有 pump／HTTP 桥为基础，能否将一次 Core 允许发送的记录、一次设备调用及其结果唯一对应，并识别错配、重复、冲突和跨运行期结果？
2. 对无法证明 API 返回与用户回应先后的迟到结果，能否稳定保存报告并输出顺序／机会资格未知，而不拿跨进程时钟数值或墙钟大小冒充顺序？不要求本轮实现更细的先后分类；它不是保存报告的前提，也不得阻塞最小验证。
3. Core／宿主分别在结果提交前、提交后重启时，能否区分“禁止补存”与“返回已提交原结果”，同时避免重复发送？

范围内：
- 隔离诊断夹具、临时数据库、最小运行标识／内存顺序记录和故障注入；字段名及结构自行选择。可以用隔离实验实现候选核对逻辑，但不得接入正式命令路由、修改生产 schema 或放宽正式回执守卫。
- 在真实桥接路径上覆盖：结果先保存；用户回应先到；请求丢失；结果已保存但响应丢失；Core 重启；宿主重启。重启分别放在提交前后；增加错许可／尝试／运行身份、同结果重试与冲突内容、仅关闭窗口的反例。
- 在可控制的诊断中区分 API 调用前、调用中、返回后的用户回应；验证保存历史结果不让已取消且尚未执行的尝试发起新调用。真实先后无法证明时按已确认规则记未知，无需继续追求更细分类；遇到超出已确认边界的情况记录并回传，不自行扩大资格。
- Linux 验证关联与顺序记录机制；如需证明实际 Windows API 行为，交付最小实机步骤并分层记录。沿用原锁定 Windows 环境，不升级依赖或虚构显示／阅读时刻。

范围外：
- 不在本轮启用 G-01 生产规则、修改领域文档或宣布 I-02 PASS；不新增宿主持久结果日志、跨重启补存、公开服务、遥测、模型功能或 I-03/I-04。

验收和退出：
- 逐支输出输入事件顺序、关联证据、可证明／未知的顺序、拟接纳／拒绝理由、重发次数及重启边界；用断言检验候选机制，保留既有 I-01 回归。诊断通过不是正式产品验收通过。
- 给出最小机制建议及其依据，明确哪些是已验证事实、哪些仅候选；未执行 Windows 的部分标 NOT_RUN，Linux PASS 不外推 Windows PASS。
- 矩阵完成即保存并回传，不自动进入生产实现；同一调试路径连续三次无新证据、发现 ADR 冲突或需扩大范围时停止并报告 BLOCKED。等待 Windows 时交付步骤及 checkpoint 后退出，不无限等待。

交付：
- 在 docs/implementation/results/i02-g01-verification.md 保存中文结果；附机器可读证据摘要、源码 commit、环境、复现命令及来源哈希，大量原始日志受控保存，不提交令牌／个人信息。
- 更新原 I-02 结果报告的入口与当前状态，保留历史 BLOCKED；更新 Issue #33、Draft PR #35 并互链。本轮仅完成有界验证，不把整个实现 PR 标为可合并。
- checkpoint commit 后 push；多行 GitHub 正文使用 UTF-8 文件及 --body-file，读回核对换行；不自行 Ready／merge／关闭 Issue。
- 回传验证结论 PASS／FAIL／BLOCKED、证据与限制、推荐机制、未覆盖项及是否发现产品／ADR 冲突。结果返回当前明确激活的治理线程核对后，再决定 G-01 正式实现授权；不自动进入下一阶段。
```

## 第二轮：宿主重启识别的最小修复复验 Prompt

使用条件：包含本节的治理 PR 经人工合并后，由用户交给既有 I-02 任务执行；不自动启动，不重复创建 session／Issue／分支。下面是完整续接 Prompt。

```text
继续现有 I-02，只执行 G-01 宿主重启识别的第二轮 technical spike；不是生产实现。你是独立技术执行 session，不继承产品治理身份。

准备：
1. 检查工作树、Issue #33、Draft PR #35，保护既有修改。保留 62bc99a／eb3863b 的失败证据，沿用 agent/implement-mvp-deterministic-recovery，不重建任务、不改写历史。
2. 核实本第二轮交接已经人工合并并存在于远端 main，再正常合入 main；未合并则返回等待。完整读取 AGENTS.md、CONTEXT.md、docs/agents/grilling-git-workflow.md、ADR-0052／0055／0057、本交接、docs/product/mvp-acceptance-scenarios.md、docs/implementation/results/i02-g01-verification.md 及其机器摘要，并检查实际实验代码。

唯一问题：如何在不依赖新宿主主动登记的情况下，拒绝旧宿主重启前未提交的设备结果，同时允许原命令取回已提交结果？

范围内：
- 只修改隔离实验及测试夹具，复用原来已通过的关联、去重、未知顺序处理。先说明候选依据如何识别同一进程实例、何时观测退出，以及与结果事务提交如何衔接，再做最小实验；可逆技术选择由你作出。
- 必须保留并修复 host_restart_before_registration 原反例，不能删除断言、让新宿主提前登记或强制 Core 一起重启来绕过它。
- 加入退出观测延迟；检查存续之后、数据库提交之前旧宿主退出且新宿主已运行但未登记；提交之后重启且响应丢失；进程身份复用／错配；仅关闭窗口但进程仍运行的控制支。进程身份复用可用确定性替身，须与真实进程退出证据分开。
- 对检查后退出的竞争给出可重复控制点，不以多跑几次没失败当作排除。若方案只能保证“Core 已观测退出之后拒绝”，如实指出它与当前边界的差异；不能把这称为已满足 ADR。
- 未能确认运行身份或存续依据时不能仅按最后登记值放行新的历史事实；报告所缺依据，不将未保存结果记成发送失败或用户未执行。原命令已提交结果的读取按既有幂等规则保留。
- 对采用的操作系统能力提供一手资料依据和目标平台限制。Linux 仅验证机制；依赖 Windows 进程行为的部分提供锁定 Windows 10 实机的最小步骤、预期、退出／清理及脱敏收集方式，未运行写 NOT_RUN。

范围外：
- 不改生产 Core schema、命令路由、回执守卫或宿主业务行为；不新增宿主持久事实库、桌面活动采集、跨重启补存或更精确时间分类。
- 不修改 ADR、产品范围或重启定义，不把进程存续字段当作原生通知显示／阅读证据；不进入 I-03/I-04，不顺带修复浏览器间歇连接问题。

验收与退出：
- 原 30 支与新增竞争分支分别输出结果；原失败应在保持原预期的前提下转绿，其他已通过分支无退化，报告不改变生产领域快照、不重发、不复活旧操作。完整回归与 Windows 证据分层记录，不用隔离库证明生产事务已完成。
- 检查之后、提交之前重启的竞争无法守住、或必须改变已确认边界时，保存反例及候选取舍并返回 FAIL／BLOCKED；不能缩小断言换取全绿。优先一个最小候选，不展开架构选型；同一路径连续三次无新证据即停。
- 矩阵完成即回传；缺 Windows 环境时交付步骤和 checkpoint 后退出。只可报告分层验证结论，不报告 I-02 PASS、不自动进入生产实现。

交付：
- 在 docs/implementation/results/i02-g01-restart-revalidation.md 保存新结果，附机器摘要、源码 commit、运行命令、环境、逐支结果、来源／原始输出哈希及限制；保留上一轮 FAIL，不覆写历史。受控保存原始日志，不提交令牌或个人内容。
- 更新原 I-02 总结果入口、Issue #33 和 Draft PR #35；checkpoint commit 后 push。多行正文用 UTF-8 文件及 --body-file，读回核对。PR 保持 Draft，不 Ready／merge／关闭 Issue。
- 返回结论、证据、哪些机制可复用、哪些仍未证明、是否需要改变产品／ADR、Windows 状态。治理线程核对后再判断下一阶段授权。
```
