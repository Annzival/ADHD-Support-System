# 第一个 MVP 构建就绪门槛

> 盘点日期：2026-08-20
>
> 当前结论：**未达到可以正式构建的状态**
>
> 维护范围：第一个可验证的端到端垂直闭环，不用于穷尽 v0.1 或未来路线图需求。

## 使用方式

本文件是产品治理检查点，不是产品领域 ADR。每完成一项已确认决定或收到一项 spike / prototype 证据后，都要更新：

1. 六项门槛的状态与证据；
2. 全部未决事项的四类归档；
3. 唯一最高优先级的主 grilling 阻塞项；
4. 可以并行启动、但不能冒充已实现功能的验证任务。

状态只使用：

- **满足**：已有明确决定和足够证据；
- **部分满足**：已有约束，但仍缺少会改变首个垂直切片的信息；
- **未满足**：尚不能据此定义或验收首个垂直切片；
- **等待验证**：问题已被正确分流，必须由独立 spike 或 prototype 提供证据。

达到所有门槛时立即暂停 grilling，并输出构建交接报告。门槛未全部满足，不妨碍独立技术验证先行；验证代码和结果不能计作 MVP 功能完成。

工作在当前产品治理线程和独立 session 之间的稳定路由、依赖与回传规则见 [MVP 构建就绪多 session 编排](mvp-session-orchestration.md)。

## 当前证据基线

- [领域语言](../../CONTEXT.md)：已经定义用户方案、下一步行动、执行会话、开始干预、未决开始界面、执行补记、恢复干预、智能体建议等统一语言。
- ADR-0001～ADR-0042：已经记录产品边界、权威状态、桌面架构、主动干预、重新进入和智能体建议的决定。
- [智能体建议场景矩阵](agent-suggestion-scenarios.md) 与 [智能体建议交互规则](agent-suggestion-interaction-rules.md)：已经区分确定性能力、Agent 建议和变更确认。
- [调研底稿](../research/adhd-execution-support-landscape.md)：提出“恢复优先的主动执行循环”等候选假设，但明确不是已经接受的产品决定。
- PR #5：已合并，智能体建议场景和呈现边界已经完成；Issue #1、#2 已关闭。
- PR #8：已合并，产品治理角色只在当前用户指令或任务 Prompt 显式激活。
- [Issue #10](https://github.com/Annzival/ADHD-Support-System/issues/10)：持续跟踪本门槛及其证据。
- [MVP 构建就绪多 session 编排](mvp-session-orchestration.md)：已经确认哪些决策留在当前产品治理线程，以及 technical spike、prototype、review 和未来 implementation 如何独立执行并回传。
- [Issue #11](https://github.com/Annzival/ADHD-Support-System/issues/11) 与 PR #13：V-01 已完成两阶段 Windows/Wails 薄宿主验证；原始总体结论为 `FAIL`，唯一失败项是置顶小窗原生关闭后的恢复。
- [Issue #14](https://github.com/Annzival/ADHD-Support-System/issues/14) 与 PR #15：V-01R 已完成最小关闭恢复修复和 Windows 10 实机复验，产品治理复审接纳 `PASS`；结合 V-01 证据后，Wails 五项桌面能力、通知上下文返回和薄宿主边界均已通过锁定环境验证。
- [Issue #17](https://github.com/Annzival/ADHD-Support-System/issues/17) 与 PR #20：V-02 已在锁定的 Windows 10 22H2 x64 环境完成 localhost HTTP/WebSocket、动态端口、一次性启动交接、临时令牌、失败拒绝和 Core 重启重连验证；产品治理复审接纳最终 `PASS`。最终验证 checkpoint 为 `73345e0`，证据记录 commit 为 `f22eae9`。
- [Issue #18](https://github.com/Annzival/ADHD-Support-System/issues/18) 与 PR #24：V-03 已完成 SQLite、Core 重启和一次 Windows PC 重启后的合成权威状态与调度恢复验证；产品治理双轴复审接纳最终 `PASS`。Windows 成功证据锚定 checkpoint `1ca7d92`，证据记录 commit 为 `bb45637`；bootstrap 超时失败路径修复 checkpoint 为 `2efbb3c`。
- [Issue #7](https://github.com/Annzival/ADHD-Support-System/issues/7)：恢复干预的具体本机载体仍未决定。

对照当前主线程已经接受的决定，尚未发现只存在于对话、但完全没有进入 ADR、`CONTEXT.md`、产品文档或 Issue 的决定。后续如果发现遗漏，应先恢复其原始确认语境，不把候选意见误写成决定。

## 门槛总览

| 门槛 | 状态 | 已有证据 | 仍缺少的构建信息 |
| --- | --- | --- | --- |
| 1. 核心假设与范围 | **满足** | 已确认一句话核心假设、设计目标、首要使用场景、首要成功行为、第一轮单用户 dogfooding 证据边界、可行性信号、归因窗口、观察停止和双层保护规则；完整方案导入与审阅是正式 dogfooding 前必须通过的独立设置切片；应用不制定底层学习或求职方案；v0.1 为 Windows 10 22H2 x64 单机应用；不以功能数量、AI 看起来智能、因果改善或医疗效果作为成功标准 | 无；B-01 已完成 |
| 2. 第一个端到端垂直闭环 | **未满足** | 调研提出“恢复优先的主动执行循环”候选；ADR 已覆盖方案导入与审阅、开始、执行、暂停、恢复、复盘等多个片段；已确认完整方案导入与审阅是 dogfooding 前必须通过的独立设置切片，并固定了该切片的退出条件、首要执行闭环交接状态，以及第一项下一步行动与计划开始时间的产生和确认规则 | 尚未分别固定两个切片的完整黄金路径；尚未裁剪必要异常路径；各步骤尚未全部形成“触发—操作—结果—退出”闭环 |
| 3. 核心领域模型 | **部分满足** | 智能体核心是唯一状态权威；用户操作、确定性规则和 Agent 建议边界已明确；模型供应商状态不能成为权威状态 | 尚未针对必要设置切片和首要执行闭环确定最小领域对象集合、状态机、命令、事件、非法转换和重启恢复语义 |
| 4. 可验证的验收标准 | **未满足** | 多项 ADR 已含方案导入、审阅、执行支持的局部规则，可作为场景来源 | 尚无必要设置切片、首要执行闭环和必要异常路径的 Given–When–Then 套件，也没有统一的功能正确判据 |
| 5. 高风险技术假设 | **满足** | V-01 / V-01R 已验证 Wails 五项桌面能力、通知上下文返回和薄宿主边界；V-02 已验证 localhost HTTP/WebSocket、动态端口、一次性启动交接、临时令牌、失败拒绝和 Core 重启重连；V-03 已验证 SQLite 事务、Core 正常与受控异常退出、幂等恢复、过期记录不补发，以及一次 Windows PC 重启后的单一宿主/Core和同一 SQLite 身份恢复 | 无；锁定环境以外、正式领域语义及实现质量仍由后续门槛和实现验收约束 |
| 6. 未决事项分类 | **满足** | 本文件已建立四类清单和升级规则，并要求持续维护 | 每次决定或验证返回后必须重新分类；非阻塞项不得重新进入主 grilling 流程 |

## 已确认的核心假设、首要成功行为、使用场景与候选核心闭环

首个 MVP 的一句话核心产品假设已经确认：

> 对已经拥有学习或求职方案、但难以按计划启动的成人 ADHD 用户，在其通过可追溯且须确认的完整方案导入与审阅建立权威用户方案后，状态持久的 PC 执行智能体能否在约定时间主动呈现已确认的下一步行动和低成本状态选择，帮助用户在计划窗口内明确进入一次执行会话，同时不造成不可接受的压力或自主感下降？

首个 MVP 的首要成功行为已经确认：

> 在计划开始时间到来后，用户通过明确操作进入一次执行会话，例如选择“立即开始”或报告“我已经开始”。

改期、今天不做或请求缩小步骤仍是已有产品边界允许的操作，但不计入这一首要成功行为。首个 MVP 不以完成一次执行会话、功能数量、AI 看起来智能或医疗效果作为首要成功指标。

首个 MVP 的首要使用场景也已经确认：

1. 用户可以看到并操作运行本应用的 Windows PC；
2. 用户已经确认一个下一步行动及其计划开始时间；
3. 到达计划开始时间时，用户尚未明确进入对应执行会话；
4. 智能体核心以 PC 作为当时可触达的主动干预载体，帮助用户进入执行会话。

下一步行动本身可以在线上或线下执行，只要求用户当时能够接收并操作 PC 上的开始干预。这是首个验证场景的前置条件，不允许系统通过桌面活动推断用户是否在场。PC 休眠、关机、用户离开 PC 后的恢复触达，以及没有计划开始时间的按需阻碍支持，不属于首要使用场景。

第一轮产品验证的证据边界已经确认：

- 项目作者本人是第一轮唯一验证用户，在自己的 Windows PC 上进行本机单用户 dogfooding；
- 该验证只判断首个闭环对这一名用户是否可用、是否产生值得继续验证的行为信号；
- 结果不得表述为对其他成人 ADHD 用户普遍有效，不据此作医疗、疗效或群体因果结论；
- 外部参与者招募、跨机器分发、遥测、隐私同意流程和群体统计推迟到后续封闭测试。

产品的设计目标仍是拥有既定学习或求职方案的成人 ADHD 用户，而不是只为项目作者定制。第一轮单用户证据用于控制 MVP 范围，不改变后续验证可迁移性的需要。

第一轮产品成功证据采用“单用户可行性信号”，已经确认记录以下维度：

- 符合首要使用场景且开始干预成功送达的机会数量；
- 用户明确进入执行会话的比例和从开始干预到进入会话的时间；
- 用户对干预是否帮助启动的主观判断；
- 用户感受到的压力和自主感是否出现不可接受的恶化；
- 闭环是否可以反复运行，而无须人工修复权威状态。

这些数据用于判断是否值得继续验证，不用于声称应用造成了个人行为改善。第一轮不要求无应用基线、前后因果比较或预设的群体转化率。

进入执行会话的行为信号按已有开始干预生命周期分为两级：

1. **即时进入**：开始干预成功送达后，用户在自己设置的开始宽限时间结束前明确进入执行会话；
2. **延后进入**：开始宽限时间结束后、ADR-0026 定义的未决开始界面失效前明确进入执行会话。

两级均记录从开始干预送达到进入执行会话的准确用时，不新增固定的 15 分钟或 30 分钟阈值。未决开始界面失效后的执行补记继续形成执行证据，但不归入这次开始干预的即时或延后行为信号。未进入仍表示没有观察到该行为信号，不得写成用户失败、拒绝或没有执行。

第一轮 dogfooding 采用低负担、有上限的观察停止规则：

1. 一次**合格机会**是指首要使用场景的前置条件成立，并且系统成功送达开始干预；
2. 观察从第一次合格机会开始；累计至少 5 次合格机会，且分布在至少 3 个不同日期后，即可正常结束；
3. 最长观察 14 个日历日，不为凑足数量额外创建下一步行动或安排开始时间；
4. 14 日结束仍未达到最低数量时同样停止，结果记为“证据不足”，不自动延期，也不记为产品失败或用户失败。

第一轮 dogfooding 同时采用压力与自主感的双层保护机制：

1. 用户可以随时无须说明理由结束本轮验证；
2. 每个出现合格机会的观察日结束时，在验证记录中分别评价压力和自主感，评价选项为“可接受”“令人不适”“不可接受”；
3. 任一维度出现一次“不可接受”，立即结束本轮验证；
4. 任一维度连续两个观察日为“令人不适”，也立即结束本轮验证；
5. 保护性停止后关闭本轮后续主动干预，保留已有证据并记为“保护性提前停止”；该结果不记为用户失败或产品失败，也不自动恢复；
6. 修改产品后需要再次验证时开启新一轮观察，不延续原轮的机会数量或日期累计。

上述评价属于 dogfooding 验证协议，不要求 MVP 首版先实现专门的量表界面。

首个可验证 MVP 采用严格核心假设边界，明确不验证：

- ADHD 诊断、治疗、症状改善或医疗效果；
- 应用是否造成个人行为改善，以及是否对其他 ADHD 用户普遍有效；
- 任务完成率、总产出、专注时长、连续执行天数或长期留存；
- 用户方案本身是否正确，或系统是否能够制定学习、求职策略；
- LLM 是否“聪明”或能否生成高质量的通用建议；
- 通用可行性分析、日终复盘、剩余精力、方案调整、多日重新进入和再触达；
- body doubling、移动端、Web/PWA、服务器模式、多用户、多设备同步和公开分发；
- 通过桌面活动推断用户是否在场、开始或完成。

完整方案导入与审阅是上述严格边界的明确例外：它纳入首个 MVP 的验证范围，并作为正式 dogfooding 前必须通过的独立设置切片。该切片必须产出经用户最终确认的权威用户方案，之后才能开始正式 dogfooding；它有独立的黄金路径与验收场景，但不计入“明确进入执行会话”的首要行为指标。开发期间可以使用预置数据先构建执行闭环，但预置数据只属于技术脚手架，不能代替正式产品验证中的完整导入与审阅。

该决定不改变“系统不制定底层学习或求职方案”的产品边界。必要设置切片遵守 ADR-0015 与 ADR-0035：保留方案原文，区分原文明确信息、Agent 派生内容和缺口，提供可保存的方案审阅工作区，并只在用户最终确认后启用权威用户方案。首要执行闭环继续从“已有且已确认的权威用户方案、下一步行动和开始时间”开始。

必要设置切片的退出条件已经确认：

1. 用户已经在确定性变更确认界面完成最终确认，智能体核心据此启用可追溯的权威用户方案；
2. 已启用方案中至少存在一个经用户确认的下一步行动及其计划开始时间，系统可以直接进入等待首个开始干预的状态；
3. 阻止形成上述可执行安排的关键缺口已经解决；其他非关键不确定信息可以继续明确保留，毋须为了通过设置切片而全部清空。

缺少最终确认、经确认的下一步行动或计划开始时间时，方案导入草案可以保存并稍后继续，但必要设置切片尚未完成，不能开始正式 dogfooding，也不能触发任务调度或主动干预。

第一项下一步行动及计划开始时间采用“原文优先、Agent 可以提议、用户逐项确认”的产生规则：

1. 方案原文已经明确给出可执行行动或时间安排时，系统将其作为带原文依据的候选；
2. 原文缺少足够具体的行动或时间时，Agent 可以在用户既有方案边界内提出任务拆分或时间分配，但必须明确标为派生内容并提供对应原文依据；不得自行新增目标、截止日期或优先级；
3. 用户可以直接编辑候选，并须在启用方案前明确确认第一项下一步行动及计划开始时间；整体确认不能代替这两个字段的明确确认；
4. 未完成明确确认时，工作区保持为可保存草案，必要设置切片尚未完成。

该规则只确定权威安排的来源和确认边界，不提前决定方案审阅工作区的视觉布局或控件形式。

这些不做项只裁剪首个可验证 MVP，不废弃已经接受的 v0.1 或未来路线图决定。执行会话收尾是否属于闭环必要步骤、阻碍支持是否使用 LLM，继续由 B-02 决定；它们不作为 B-01 的首要成功指标。

以下黄金路径仍是候选，须完成 B-02 后才能成为接受范围：

```text
已有且已确认的下一步行动
  → 用户确认执行时间
  → 到点产生开始干预
  → 用户选择立即开始，或在阻碍支持中接受一个缩小后的步骤
  → 建立执行会话与一个会话检查点
  → 用户报告完成或暂停并保存恢复包
  → 收尾确认保存执行证据并退出本次闭环
```

候选必要异常仅包括：

- 用户未回应开始干预，系统停止主动跟进并保留/过期正确的被动入口；
- 模型不可用，确定性开始、改期、暂停和退出操作仍然可用；
- 智能体核心或 PC 重启，权威状态和调度可恢复，过期干预不补发；
- 非法或基于旧版本状态的转换被拒绝，权威状态不被破坏。

多日重新进入、日终复盘、剩余精力、邮件再触达等已确认能力是否进入后续切片，不影响上述候选闭环能否先被构建和验证。完整方案导入与审阅已经明确为 dogfooding 前必须通过的独立设置切片，不与下述可重复执行闭环合并计算行为信号。

## 全部未决事项的四类归档

### MVP 阻塞

这些问题会改变首个垂直切片，只有它们可以占用主 grilling 流程，并严格一次处理一个：

B-01 已完成：一句话核心产品假设、成功标准、明确不做项、首要成功行为、首要使用场景、第一轮证据边界和完整方案导入与审阅的必要设置切片地位均已确认。它不再占用主 grilling 流程。

| ID | 未决事项 | 为什么阻塞 | 解决方式 |
| --- | --- | --- | --- |
| B-02 | 分别固定必要设置切片和首要执行闭环的黄金路径，并裁剪最小必要异常集；必要设置切片的退出条件、交接状态及第一项安排的产生和确认规则已经确认 | 尚未固定两个切片的完整步骤、必要异常和剩余裁剪边界 | **继续 grilling**；当前最高优先级 |
| B-03 | 为 B-02 的两个切片建立最小领域对象、权威状态、命令、事件和状态转换 | 实现者无法区分草案与权威用户方案、合法执行转换、未知状态和恢复状态 | B-02 后使用 domain-modeling；必要时新增产品状态模型，只有满足 ADR 条件时才新增 ADR |
| B-04 | 把两个黄金路径与必要异常路径写成 Given–When–Then 验收场景 | 无法判断实现是否正确，也无法约束 spike 之后的 implementation | B-02、B-03 后编写验收文档 |

B-02 明确包含 ADR-0026 尚未解决的上下文竞争：首个垂直闭环是否允许“下一项计划工作已经开始，但上一项未决开始界面尚未到保留期限”的并存状态。如果黄金路径允许并存，B-02 必须把置顶小窗如何选择当前上下文纳入必要异常或转交交互原型；如果首个闭环只允许一个当前执行安排，该呈现问题归入 MVP 后。

### 实现中再决定

下列选择可逆、成本较低，或不会改变首个闭环，不得阻塞开始编码：

- Wails 前端所用的具体组件库、CSS 方案、目录组织和页面布局；
- 主窗口、置顶小窗和卡片的视觉样式、图标、动画细节及非关键文案；
- HTTP 资源命名、WebSocket 消息命名、Python 包布局和内部模块边界；
- SQLite 访问库、迁移工具、表名、索引和日志轮转细节；
- 端口和临时令牌的具体编码格式，以及验证通过后的默认配置值；
- 模型供应商、模型名称、API key 配置界面和开发期默认模型；权威状态与供应商无关的约束不变；
- 安装包、自动更新和诊断日志导出体验；在首次可运行切片之后、公开分发之前补齐；
- 开源许可证、贡献模板、公开 demo 与作品集包装；必须在对外宣称可复用的开源发布前决定，但不阻塞首个闭环编码；
- 成功指标的埋点存储和可视化方式；指标定义本身属于 B-01，不能延后。

### 需要技术或交互原型验证

这些事项可能阻塞构建就绪门槛，但不得继续占用纯讨论；每项都由独立 session、Issue、分支和 Draft PR 产出证据：

| ID | 验证事项 | 当前能否开始 | 与产品边界的关系 |
| --- | --- | --- | --- |
| V-01 | Windows 10 22H2 x64 上 Wails v3 的托盘常驻、置顶小窗、开机启动、可交互通知和 Python 进程守护（Issue #11、#14） | **已完成**：V-01R 修复唯一失败项后，组合证据经产品治理复审为 `PASS` | 结论只适用于锁定的目标环境和单台实机；Wails 不承担领域调度、权威状态或 LLM，不计作 MVP 功能完成 |
| V-02 | Wails / 桌面客户端与 Python 智能体核心的 localhost HTTP/WebSocket、端口发现和临时令牌（Issue #17） | **已完成并经产品治理复审接纳为 `PASS`** | 只证明锁定环境中的最小假数据协议与启动交接可行，不替 B-03 决定正式领域 API，也不计作 MVP 功能完成 |
| V-03 | SQLite 持久化、智能体核心重启和 PC 重启后的状态、调度与失效干预恢复（Issue #18、PR #24） | **已完成并经产品治理双轴复审接纳为 `PASS`** | 仅证明锁定环境中最小合成机制可行；领域含义最终受 B-03 约束，不计作 MVP 功能完成 |
| V-04 | 系统通知操作能否返回正确的本机应用上下文 | **已与 V-01 一并完成** | 设备桥接和演示上下文返回已通过；桌面宿主不保存权威上下文状态 |
| V-05 | 恢复干预在置顶小窗、系统通知和主窗口之间的本机载体映射（Issue #7） | **等待 B-02 确认恢复路径是否属于首个闭环后做低保真交互原型** | 固定操作是确定性能力；需要缩小步骤时才进入阻碍支持 |
| V-06 | 如果黄金路径包含阻碍支持，真实 LLM 生成的首个缩小步骤是否足够小、可执行且不越权 | **等待 B-02 后定义 prototype / eval** | LLM 只能生成建议，不能直接修改权威状态 |
| V-07 | 如果首个闭环包含 LLM，PydanticAI 模型供应商切换、模型不可用回退和权威状态独立性 | **等待 B-02、B-03 和 V-06 后定义 technical spike** | 更换 API key 或模型供应商不得丢失状态；模型不可用时确定性操作仍可执行 |

V-01 与其最小关闭恢复复验 V-01R 已完成。产品治理线程结合 PR #13 和 PR #15 的结果接纳：Wails 五项桌面能力、通知上下文返回和薄宿主边界在锁定环境中具有足够的实机 `PASS` 证据，可以继续保留 Wails v3 作为 v0.1 首选桌面宿主。该结论不外推到其他 Windows、Wails 或 WebView2 版本，也不把 spike 代码计作 MVP 功能完成。

产品治理线程已经结合 Issue #17、PR #20、Windows 实机证据摘要和两轮独立复审接纳 V-02 的最终 `PASS`。该结论只覆盖同机同用户会话中的最小 localhost 假协议启动交接：Core 只监听动态回环端口，Wails 通过一次性 bootstrap file 获得每运行临时令牌，并完成认证 HTTP / WebSocket、错误与旧令牌拒绝、超时 / 不可用非就绪和一次 Core 重启后的重新连接。它不声称防御同权限恶意本地进程，不外推到锁定版本之外，也不定义正式领域 API。

产品治理线程已结合 Issue #18、PR #24、Windows 实机脱敏证据及两轮双轴复审接纳 V-03 的最终 `PASS`。该结论覆盖锁定环境中的最小合成 SQLite 事务与恢复机制：已提交状态在正常与受控异常退出后保持一致，未提交写入不形成半更新；未来动作与合并恢复信号保持幂等，过期记录不补发；一次真实 PC 重启后只出现一个宿主和一个 Core，并从同一 SQLite 身份恢复。Windows 成功证据继续且仅锚定 `1ca7d92`；后续 `2efbb3c` 只修复并自动验证 bootstrap 超时失败路径，没有被表述为新的 Windows 实机证据。

V-03 不覆盖异常断电、数据库加密、备份恢复、其他 Windows 版本、多用户或多设备，也不定义 B-03 的正式领域对象、状态或失效语义。结合 V-01/V-01R、V-02 与 V-03，门槛 5 的既定高风险技术验证已经满足；这些 spike 代码和证据仍不计作 MVP 功能完成。

### MVP 后

以下能力保留既有决定或路线图价值，但不应阻塞第一个垂直闭环：

- Web/PWA、手机原生客户端、多端同步、多用户、公网访问和常开自托管服务器；
- Windows 11、其他 Windows 架构、macOS 和 Linux 的支持承诺；
- 真人或 AI body doubling；
- 微信、QQ 等即时通讯渠道及其账号、审核、限流和送达集成；
- 关机期间仍可靠送达的邮件再触达、邮件深链和跨设备认证；v0.1 已接受的本机尽力发送规则继续保留；
- ADR-0021 中准备提示的具体本机呈现形式，包括是否使用置顶小窗；以及微信、QQ 等外部载体和更多提醒渠道。准备提示本身不进入候选的第一个垂直闭环；
- 跨方案通用可行性分析；完整方案导入与方案审阅工作区已经作为严格范围边界的明确例外纳入首个 MVP 验证，不属于本分类；
- 完整日终复盘与三天补记入口、剩余精力建议、方案调整建议的跨场景工作流；
- 多日执行空档、期间执行核对、完整邮件再触达序列；智能体核心或 PC 重启后的当前状态恢复仍属于必要异常；
- 全局待决定建议汇总、复杂建议撤销和补偿性调整历史界面；供应商无关建议记录和 LLM 不直接写状态的边界继续保留；
- 桌面活动采集或 ActivityWatch 扩展；v0.1 不采集的决定不变；
- 面向其他地区或医疗用途的合规研究、危机资源本地化和疗效主张；首个 MVP 不作医疗效果承诺。

将这些能力放入“MVP 后”不废弃已经接受的 v0.1 或演进路线决定，只表示它们不进入第一个可验证垂直闭环。黄金路径确认后如发现其中某项确实是闭环必要条件，必须写明证据后再重新分类。

## 当前唯一最高优先级阻塞项

**B-02：分别固定必要设置切片和首要执行闭环的黄金路径，并裁剪最小必要异常集。**

门槛 1、5、6 已经满足，B-01 与既定高风险技术验证完成。必要设置切片现已确定退出条件，并采用“原文优先、Agent 可以提议、用户逐项确认”的规则产生第一项下一步行动及计划开始时间；非关键不确定信息可以保留。为了让实现者能够构建第一个垂直切片，仍须固定两个切片的完整步骤，并只保留确实影响闭环正确性的异常路径。未完成这些决定，就无法为后续领域模型和验收场景提供稳定边界。

解决方式是继续一次只问一个问题的 grilling。V-03 已经完成并经产品治理接纳，只消除了门槛 5 的技术不确定性，不替 B-02～B-04 作产品决定。

## 达到构建就绪后的交接要求

六项门槛全部满足后，构建交接报告至少包含：

- 支持判断的文档、ADR、Issue、spike 和 prototype 证据；
- MVP 精确范围、不做项、黄金路径和必要异常路径；
- 最小领域对象、权威状态、命令、事件和状态转换；
- Given–When–Then 验收场景；
- 尚存但不阻塞构建的风险；
- 第一个端到端实现切片、推荐顺序、每阶段完成条件和验收命令；
- 可推迟到实现反馈出现后的决定。
