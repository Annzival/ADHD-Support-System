# 每个执行会话使用一个约定检查点

执行会话开始时，系统根据用户确认的预计时长建立一个会话检查点；检查点到达前不按固定周期发送“是否仍在专注”等提示，用户可以随时主动报告卡住、完成或需要暂停。检查点到达后，系统提供完成、继续并确认新检查时间、请求阻碍支持以及暂停并保存恢复包四类操作。

## 无回应

用户没有回应会话检查点时，执行状态保持未确认，系统最多再进行一次低强度确认，随后停止追问。计时结束本身不能把任务自动标记为完成或失败。
