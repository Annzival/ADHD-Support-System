# 等待收尾期间继续占用唯一活动会话位置

执行会话与首次检查点原子建立后进入执行中并占用唯一活动会话位置。用户报告完成或暂停时，检查点失效，会话进入等待收尾并继续占用该位置；只有用户完成或跳过收尾确认，系统原子地保存最小执行证据和必要的恢复包、结束会话后才释放。等待收尾期间不得建立或恢复第二个执行会话。会话检查点无回应达到既定期限时不进入收尾，而是记录会话跟踪结束和未确认执行状态后释放位置，不把应用停止跟踪伪装成现实执行已经结束。
