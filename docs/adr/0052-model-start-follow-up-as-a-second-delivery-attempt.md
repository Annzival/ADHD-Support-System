# 将较弱跟进建模为同一开始干预的第二次送达尝试

每个执行安排最多创建一个逻辑开始干预，首次主动呈现是第一次干预送达尝试，宽限时间结束后的一次较弱跟进是同一干预的第二次且最后一次尝试；多个载体呈现同一上下文不增加领域干预数量。用户从任一载体回应都解决同一个开始干预，未决开始界面只是未解决状态的被动投影。回应、行动状态改变或安排失效会使未执行尝试失效，离线错过的尝试不补发；一次 dogfooding 合格机会按逻辑开始干预成功送达计算，重试不得创建第二个干预或重复解决记录。
