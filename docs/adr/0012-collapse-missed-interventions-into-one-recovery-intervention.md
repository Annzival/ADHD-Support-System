# 将错过的干预合并为一次恢复干预

当智能体核心在启动或唤醒后发现已经错过有效时机的干预时，将它们记录为失效干预，不逐条补发，也不静默删除。系统改为根据当前时间、用户方案和最新执行状态生成至多一次恢复干预，让用户确认已经完成、正在执行、现在开始缩小后的步骤、改到具体时间，或今天不再执行。

## 影响

历史记录需要区分“已送达”和“因离线而失效”，任务调度也必须从当前状态重新计算，不能把通知队列当作等待依次发送的命令队列。这可以保留复盘证据，同时避免用户重新上线后收到过时通知形成的堆积。
