# 将用户设置的干预边界视为硬约束

用户决定允许主动联系的时间段、启用的渠道，并可以暂停全部干预到指定时间；这些设置构成智能体核心不可越过的干预策略。安静时段没有由 Agent 判断重要性后自行突破的例外，邮件再触达也受相同规则约束。

## 影响

智能体核心可以因为没有回应、任务状态变化或已经完成而减少或取消干预，但不能自行增加频率、启用新渠道或延长允许联系的时段，只能提出修改建议并等待用户确认。如果任务安排与干预策略冲突，系统提出方案调整建议，而不是绕过用户设置。
