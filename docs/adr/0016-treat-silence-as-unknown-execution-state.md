# 将沉默视为执行状态未知

计划开始干预送达后，如果用户没有回应，智能体核心只能记录未确认执行状态，不得推断用户未开始、拒绝执行或执行失败。每次计划开始最多再发送一次后续确认；再次没有回应时停止追问，也不得因沉默自动修改用户方案或当天执行安排。

## 影响

系统需要分别保存干预已送达和用户未回应这两个事实。用户以后再次交互时，系统再请其核对实际结果并补记已经完成、曾经执行、没有开始或已经改期，而不是把缺失信息预先写成失败记录。
