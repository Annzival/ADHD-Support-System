# 通过本机回环 API 隔离智能体核心

在 v0.1 中，桌面客户端只能通过 localhost 上的 HTTP/WebSocket API 与智能体核心通信，绝不直接访问 SQLite。智能体核心只监听本机回环接口，其连接地址集中配置，通信协议不依赖 Wails。桌面宿主可以管理操作系统进程生命周期、本地端口发现和临时认证令牌，但领域操作不得绕过 API。

## 影响

本机智能体核心是 v0.1 唯一的状态权威。未来将智能体核心迁移到自托管 Linux 服务器或增加其他客户端时，可以复用这一协议边界；首版不实现公网通信、远程部署或同步。
