# 导入的方案必须经用户确认后才能生效

用户通过文本或 Markdown 提供已有方案，系统保留方案原文，并由 Agent 生成方案导入草案。草案必须区分原文明确给出的内容、Agent 派生的任务拆解或时间安排，以及缺失、冲突或无法确定的信息；Agent 不得擅自补充新的目标、截止日期或优先级，只能请用户补充或确认假设。

## 影响

只有用户确认后的结构化版本才能成为智能体核心中的权威用户方案，并进入任务调度和主动干预。确认前的草案不触发执行支持；系统需要保留原文、派生内容和用户确认之间的对应关系，使 Agent 是否越过方案边界可以被审查。
