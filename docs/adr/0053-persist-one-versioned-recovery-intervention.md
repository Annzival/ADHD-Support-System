# 持久化一个带来源版本的恢复干预

智能体核心启动或唤醒后，以当前权威状态确定是否创建至多一个未解决恢复干预，并保存其所引用的失效干预、活动会话或恢复包、当前执行安排、适用行动、相关状态版本和当时由确定性规则允许的操作。主窗口和系统通知只是该记录的载体投影；状态未变化时重启或重复检查复用同一记录。来源状态变化会使旧恢复干预失效，仍需支持时再创建新记录；用户操作必须重新校验来源版本，过期操作被拒绝。解决恢复干预只记录选择结果并执行对应领域转换，不吞并、覆盖或删除来源记录，也不调用 LLM。
