# 在同一执行会话内串联不可回写的检查点

每个会话检查点表示一次经用户确认的具体检查时间。用户选择继续并确认下一检查时间时，系统原子地把当前检查点记录为已回应并继续，同时在同一执行会话内创建唯一后继检查点；不覆盖原检查时间，也不结束后重建执行会话。已处理检查点保留计划时间、送达情况和用户选择，但不得再次触发；无回应不能自动产生后继检查点，重复提交同一次继续操作必须幂等，不能产生两个后继检查点。
