# 在既有边界自动跳过长期无回应的收尾

等待收尾不再触发主动提醒、邮件或较弱跟进；用户关闭收尾界面视为明确跳过。没有操作时，系统在原执行窗口结束、下一项计划工作需要成为前台执行上下文、或没有前两者时用户本地时区次日 00:00 三个适用边界中取最早者，确定性地自动跳过收尾。该转换只依据此前明确的完成或暂停操作保存最小执行证据和已有的最小恢复包，不推断实际用时、额外完成程度或剩余精力，并原子地结束会话和释放活动位置；重启后必须恢复未到期状态或对已到期状态幂等执行一次。
