# 执行支持

本上下文服务于用户执行已有的学习或求职方案，负责执行支持闭环，不负责制定方案的策略或内容。

## 领域语言

**用户方案（User Plan）**:
用户在执行支持开始前已经制定，并在导入后确认其系统表示的具体学习或求职方案；它是系统派生可执行工作的权威依据。
_避免使用_: 智能体方案、自动生成方案

**用户方案版本（User Plan Version）**:
用户最终确认后发布的不可变用户方案表示；当前版本是未来执行支持的权威依据，历史执行记录引用当时适用的确切版本。
_避免使用_: 可编辑方案、当前草案、覆盖后的方案

**方案原文（Plan Source）**:
用户导入系统的已有方案原始内容，用于辨认哪些信息来自用户、哪些内容由 Agent 派生。
_避免使用_: 提示词、导入草稿

**方案导入草案（Plan Import Draft）**:
Agent 从方案原文中整理出的非权威结构化表示，其中明确区分原文信息、派生的拆解或时间安排，以及仍待用户解决的缺口。
_避免使用_: 已生效方案、Agent 方案

**方案审阅工作区（Plan Review Workspace）**:
用户检查和编辑方案导入草案、追溯原文依据并解决关键缺口的可保存主窗口界面。
_避免使用_: 导入聊天、自动建计划、强制逐项问答

**方案启用（Plan Activation）**:
用户最终确认一个用户方案版本，使其成为权威依据并允许执行支持开始的领域转换。
_避免使用_: 保存草案、Agent 发布、自动生效

**执行智能体（Execution Agent）**:
帮助用户把用户方案转化为可执行工作，并支持启动、持续执行、恢复和复盘的 AI Agent 应用。
_避免使用_: 规划器、职业教练、学习教练

**智能体核心（Agent Core）**:
执行智能体中持续运行且拥有权威状态的部分，负责执行状态、任务调度和何时提供支持的决策。
_避免使用_: Agent Service、用“后端”指代其领域职责、模型会话

**桌面客户端（Desktop Client）**:
v0.1 中唯一面向用户的 PC 交互界面，负责呈现智能体核心中的状态并向其发送用户操作，但不拥有权威执行状态。
_避免使用_: 通用客户端、Web 客户端、第二状态源

**桌面宿主（Desktop Host）**:
可替换的 PC 本机适配层，负责守护智能体核心进程，并桥接操作系统的界面呈现和生命周期能力；它不拥有执行状态或支持决策。
_避免使用_: 桌面后端、独立桌面产品

**方案调整建议（Plan Change Proposal）**:
系统在方案审阅、执行支持、复盘或重新进入等上下文中，根据可行性、时间安排或执行证据，对用户方案提出的不具约束力的调整建议。
_避免使用_: 自动重新规划、方案重写

**通用可行性分析（General Feasibility Analysis）**:
对已启用用户方案的截止日期、依赖关系、可用时间、估时和执行证据进行的按需或关键变化触发分析。
_避免使用_: 策略规划、成功预测、自动重新规划

**智能体建议（Agent Suggestion）**:
Agent 根据带版本的应用状态生成、由应用持久化并等待用户决定的结构化非约束性提议，包含依据、假设、影响和有效性边界。
_避免使用_: LLM 回复、Agent 命令、自由文本即权威状态

**建议生命周期（Suggestion Lifecycle）**:
智能体建议在用户决定、业务有效性和注意状态三个独立维度上的变化规则。
_避免使用_: 单一建议状态、未读任务、建议债务

**建议注意请求（Suggestion Attention Request）**:
系统为了让用户查看一项仍然有效的智能体建议而主动提高其显著程度的一次呈现，同一时刻最多存在一项。
_避免使用_: 建议通知队列、连续弹窗、Agent 自定最高优先级

**待决定建议汇总（Pending Suggestion Summary）**:
首页对仍然有效且等待用户决定的智能体建议所作的简要分组导航，不是建议的权威入口或待办收件箱。
_避免使用_: 建议中心、未读建议、建议任务列表

**确定性操作面板（Deterministic Action Surface）**:
由领域规则立即生成、用于明确状态转换的操作界面，不依赖 LLM 推理或生成建议。
_避免使用_: Agent 建议、智能回复、模型操作

**智能体建议卡片（Agent Suggestion Card）**:
用于呈现一项范围明确的 Agent 建议、简短依据、影响和用户确认操作的非模态界面。
_避免使用_: 自动决策、通知正文、建议队列债务

**上下文对话（Contextual Agent Conversation）**:
在主窗口中围绕当前用户方案、下一步行动或执行状态进行的多轮 Agent 交互，用于处理缺失信息或需要共同澄清的问题。
_避免使用_: 无上下文聊天、强制模态框、模型会话即领域状态

**变更确认（Change Confirmation）**:
用户决定采用智能体建议后、修改用户方案或当天执行安排前，由确定性界面呈现变更差异和影响的最终确认步骤。
_避免使用_: Agent 二次建议、自动应用、不可取消确认

**建议应用记录（Suggestion Application Record）**:
智能体建议被接受后，应用保存的来源建议、变更前后状态、最终应用内容和领域命令结果记录。
_避免使用_: 聊天结果、可删除历史、模型写入日志

**阻碍支持（Blocker Support）**:
用户表示难以开始或继续时，由系统提供确定性退路和行动优先的 Agent 建议、以降低当前执行阻力的支持流程。
_避免使用_: 原因审问、心理诊断、强制问题清单

**下一步行动（Next Action）**:
在确切用户方案版本中定义、准备执行的最小可观察工作单元；它描述要做什么，不承载计划执行时间。
_避免使用_: 在特指立即执行的行动时使用“任务”

**执行安排（Execution Arrangement）**:
为一个下一步行动约定的计划执行机会，包含计划开始时间以及可选的执行窗口和预计时长；改期产生引用同一行动的后继安排，不覆盖原安排。
_避免使用_: 下一步行动、日程任务、修改后的原安排

**本次不执行决定（Skip Arrangement Decision）**:
用户明确选择不按当前执行安排继续执行的时间相关事实；它结束本次安排，但不表示下一步行动失败、完成或被永久放弃。
_避免使用_: 任务失败、永久放弃、没有执行证据

**执行会话（Execution Session）**:
一次有明确边界、旨在推进某个下一步行动并产出可观察进展的执行尝试。正常启动的会话与首次检查点原子建立；回顾式执行会话可以从等待收尾开始。
_避免使用_: 专注连胜、工作块

**回顾式执行会话（Retrospective Execution Session）**:
用户在尚无应用内执行会话时明确报告行动已经完成，由系统直接建立为等待收尾的执行会话；它没有检查点，现实开始和结束时间保持未知，也不计入开始干预促成的进入行为。
_避免使用_: 伪造会话、自动完成、正常启动会话

**会话检查点（Session Checkpoint）**:
执行会话中约定的一次不可回写状态核对边界，用于决定完成、继续、求助或暂停；继续执行会建立同一会话内的后继检查点，而不是覆盖原时间或周期性盘问用户。
_避免使用_: 专注抽查、循环提醒、自动结束

**前台执行上下文（Foreground Execution Context）**:
强可见载体在同一时刻优先呈现的唯一执行上下文；多个未决记录可以在权威状态中并存，但不同时争夺用户注意力。
_避免使用_: 唯一任务、覆盖旧任务、多任务提醒队列

**收尾确认（Closure Check）**:
执行会话结束时用于确认结果、实际用时、恢复需要和剩余精力的可跳过简短核对，不承担完整复盘。
_避免使用_: 强制复盘、完成报告、绩效检查

**等待收尾（Awaiting Closure）**:
用户已经报告完成或暂停、会话检查点已经失效，但执行会话仍等待完成或跳过收尾确认的状态；它继续占用唯一活动会话位置。
_避免使用_: 仍在执行、会话已结束、待复盘

**自动跳过收尾（Automatic Closure Skip）**:
等待收尾未获操作并到达确定性结束边界时，系统只依据此前明确的完成或暂停操作保存最小执行证据并结束会话的转换。
_避免使用_: 自动完成、超时失败、推断式收尾

**日终复盘（Daily Review）**:
用户在自己设定的复盘窗口内，对当天执行证据和用户方案影响进行的可选整体回顾。
_避免使用_: 每日报告、强制总结、绩效复盘

**补记日终复盘（Catch-up Daily Review）**:
主动复盘窗口结束后 72 小时内，由用户通过主界面的被动入口自行发起的日终复盘。
_避免使用_: 复盘债务、逾期复盘提醒、补交

**执行证据（Execution Evidence）**:
用户明确报告或在执行智能体中采取可解释操作后形成的不可回写事实，用于复盘和分析用户方案的可行性；沉默和桌面活动不属于执行证据。
_避免使用_: 活动轨迹、隐式生产力评分、Agent 猜测

**执行事实更正（Execution Fact Correction）**:
用户纠正已经保存的历史执行事实时追加的记录；它说明更正内容和原因，但不覆盖原执行证据。
_避免使用_: 编辑历史、覆盖记录、静默修复

**恢复包（Resume Packet）**:
暂停的执行会话结束时固化的一次性接续依据，引用当时的方案版本、下一步行动、会话和已取得进展；它可以被一个接续会话使用，或在用户选择当前计划时未使用归档。
_避免使用_: 聊天记录、摘要

**干预（Intervention）**:
系统主动发起的状态确认或支持提议，用于帮助用户启动、继续、恢复或复盘工作。
_避免使用_: 催促；在指支持决策而非传递渠道时使用“通知”

**准备提示（Preparation Cue）**:
在计划开始前告知用户即将切换到下一步行动的可选低侵入提示，不要求用户立即开始或作出回应。
_避免使用_: 正式提醒、开始干预、任务命令

**开始干预（Start Intervention）**:
下一步行动到达计划开始时间时，针对一个执行安排创建、用于让用户低成本确认实际状态或选择后续处理的一次逻辑干预；首次呈现和一次较弱跟进只是它的送达尝试。
_避免使用_: 开始闹钟、开工质询、将“已经开始”和“已经完成”合并为“已处理”

**干预送达尝试（Intervention Delivery Attempt）**:
系统为了主动呈现同一个逻辑干预而进行的一次有时间边界的送达；多个界面载体可以承载同一次尝试，但不会增加领域干预数量。
_避免使用_: 新干预、通知即干预、被动界面

**未决开始界面（Pending Start Surface）**:
开始干预没有获得明确回应后，在当前执行窗口结束前被动保留原有操作的界面；它不产生新的主动联系。
_避免使用_: 持续干预、第三次跟进、逾期提醒

**执行补记（Execution Backfill）**:
当前执行窗口结束后，由用户以历史状态措辞补充实际执行结果的被动入口。
_避免使用_: 补任务、任务债务、逾期回应

**期间执行核对（Period Execution Reconciliation）**:
逐项执行补记已经过期或发生多日执行空档后，由用户以期间概况补充执行事实的可跳过核对。
_避免使用_: 逐项追债、历史审计、失败清算

**干预策略（Intervention Policy）**:
由用户控制、限定智能体核心可以在什么时间以哪些渠道主动联系用户的规则边界。
_避免使用_: 提醒偏好、Agent 自主权限、可忽略设置

**干预策略冲突（Intervention Policy Conflict）**:
执行安排需要的主动支持落在干预策略禁止联系的时间或渠道内，而行动本身仍可由用户主动执行的状态。
_避免使用_: 不可执行任务、提醒权限不足、紧急例外

**未确认执行状态（Unconfirmed Execution State）**:
开始干预或会话检查点已经送达，但尚未获得用户明确执行结果时对事实未知的领域表述；它不等同于执行证据、完成、失败、拒绝或暂停。
_避免使用_: 未执行、执行失败、拒绝执行、沉默证据

**会话跟踪结束（Session Tracking Ended）**:
会话检查点长期未获明确回应并达到保留期限后，应用停止占用活动会话位置的事实；它不表示现实执行已经结束，也不形成完成、暂停或失败结论。
_避免使用_: 会话完成、自动暂停、现实执行结束

**执行空档（Execution Gap）**:
用户事后确认的一段没有执行原定工作的时期，用于评估方案受到的影响，而不是评价用户或给任务累计失败次数。
_避免使用_: 失败期、任务债务、拖延期

**重新进入流程（Re-entry Flow）**:
执行空档结束后，用于核对事实、评估方案影响并由用户选择新执行入口的一次支持过程。
_避免使用_: 补进度、追赶流程、惩罚性复盘

**失效干预（Expired Intervention）**:
因设备离线、休眠或智能体核心停止运行而错过有效时机，不应再向用户送达的干预。
_避免使用_: 逾期提醒、待补发通知

**恢复干预（Recovery Intervention）**:
设备或智能体核心恢复运行后，系统依据当前权威状态生成的一次性持久支持记录；它引用失效干预、恢复包和当前执行上下文，但不吞并这些来源事实。
_避免使用_: 补发提醒、通知摘要

**再触达序列（Re-engagement Sequence）**:
用户持续没有回应时，经其预先启用、用于重新建立联系的有限干预序列；它不重复具体任务提醒，并在用户回应或达到停止点后结束。
_避免使用_: 催促升级、邮件轰炸、任务补发

**剩余精力（Capacity Surplus）**:
用户完成计划内工作后报告的剩余时间与精力，它们可能使追加工作成为可行选择。
_避免使用_: 多余生产力、闲置容量

**剩余精力建议（Capacity Surplus Suggestion）**:
用户报告剩余精力后，Agent 从已确认用户方案中提出的有限追加工作候选，并始终将到此结束保留为平等选择。
_避免使用_: 自动填满时间、最大化产出、未利用精力

**陪伴执行者（Body Double）**:
在执行会话中陪伴用户、但不接管用户工作的真人或软件角色。
_避免使用_: 监督者、任务负责人
